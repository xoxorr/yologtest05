// lib/screens/auth/password_protect.dart
import 'package:flutter/material.dart';

class PasswordProtect extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('비밀번호 보호'),
      ),
      body: Center(
        child: Text('비밀번호 보호 기능이 여기에 구현됩니다.'),
      ),
    );
  }
}
