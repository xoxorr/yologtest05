import 'package:flutter/material.dart';

class MediaEditorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('미디어 편집기'),
        backgroundColor: Color(0xFF003366),
      ),
      body: Center(
        child: Text('미디어 편집 기능이 여기에 추가됩니다.'),
      ),
    );
  }
}
